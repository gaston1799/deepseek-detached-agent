# Dummy Smoke Files

This folder is safe test data for DeepSeek tool smoke tests.

Useful checks:
- Search for `SMOKE_TARGET`.
- Glob for `**/*.js` or `**/*.md`.
- Read multiple files from `src/`.
- Patch `PATCH_ME`.
- Inspect related imports from `src/app.js`.

